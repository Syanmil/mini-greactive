
let data = {
  hp: 100,
  sword: 10,
  shield: 10,
  spear: 10,
  axe: 10
}

export default (state = data, action) => {
  switch (action.type) {
    case 'expression':

      break;
    default:
      return state
  }
}
